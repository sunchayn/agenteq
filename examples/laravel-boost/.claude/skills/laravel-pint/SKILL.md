---
name: laravel-pint

description: Formats PHP code with Laravel Pint before a commit.
---

# Laravel Pint

Run `vendor/bin/pint --dirty` before committing. It formats only the files changed since the last commit, so it stays fast on a large codebase.
